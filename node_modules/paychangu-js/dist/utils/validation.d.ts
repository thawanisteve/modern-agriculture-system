import { PaychanguConfig, LevelConfig } from '../core/types';
export declare function validateConfig(config: PaychanguConfig): void;
export declare function validateLevelConfig(config: LevelConfig): void;
