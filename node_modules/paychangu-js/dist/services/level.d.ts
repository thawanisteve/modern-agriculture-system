import { LevelConfig, LevelResponse } from '../core/types';
export declare class LevelAPI {
    private static instance;
    private secretKey;
    private constructor();
    static getInstance(): LevelAPI;
    setSecretKey(secretKey: string): void;
    initiateTransaction(config: LevelConfig): Promise<LevelResponse>;
}
