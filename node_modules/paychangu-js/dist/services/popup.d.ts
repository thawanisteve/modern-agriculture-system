import { PaychanguConfig } from '../core/types';
declare global {
    interface Window {
        PaychanguCheckout: (config: PaychanguConfig) => void;
    }
}
export declare class PaychanguPopup {
    private static instance;
    private scriptLoaded;
    private constructor();
    static getInstance(): PaychanguPopup;
    private loadScript;
    initialize(config: PaychanguConfig): void;
    makePayment(): Promise<void>;
}
